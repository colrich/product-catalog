platform: linux

image_resource:
  type: docker-image
  source:
    repository: airdock/oracle-jdk
    tag: latest

inputs:
  - name: source-code

outputs:
  - name: source-code

run:
  path: "source-code/create-services.sh"
  args: ["{{cf-api}}", "{{cf-user}}", "{{cf-pass}}", "{{cf-org}}", "{{cf-space}}", "{{jrc-eureka}}"]

params:
  MAVEN_BASEDIR: source-code/product-service
